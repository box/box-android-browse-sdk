package com.box.androidsdk.browse.fragments;

/**
 * Listener that can be added to a browse fragment, to be notified when the fragment updates
 * itself with new data.
 */
public interface OnUpdateListener {

    /**
     * Method called when the fragment content is updated
     */
    public void onUpdate();
}
